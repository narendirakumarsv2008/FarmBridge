"""FarmBridge models package.

Models are lightweight helpers around the shared database rows. The database
layer remains the source of truth; these helpers keep naming and serialization
consistent across services and routes.
"""
